# `_`-prefixed files are the shared libraries the other cases in this directory
# import; run_sema_tests.py (beside them) skips them for the same reason. They are
# corpus, not tests, so lit must not collect them.
config.excludes = ["_lib_a.k", "_lib_b.k"]
