# A multi-file CORPUS, not a test. `main.k` imports the `geo` module tree to
# give EmitPlan a diamond (render and text both reach vec), and `consumer.cc`
# is the C++ side that includes the generated headers. None of it carries a
# RUN line -- compiled standalone every file asserts nothing, and lit would
# report each one as UNRESOLVED.
#
# Drive it by hand against a built kairo (--print-emit-plan, --print-cxx,
# --emit-headers). A file here that becomes a real test gets a RUN line and
# moves out.
config.suffixes = []
