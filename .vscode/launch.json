{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "Run Kairo File",
        "request": "launch",
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "build-kairo-current",
        "stopAtEntry": false,
        
        // Defaults (non-Windows → cppdbg)
        "type": "cppdbg",
        "program": "${workspaceFolder}/build/.gen/test_run",
        "MIMode": "gdb",
        "miDebuggerPath": "/usr/bin/gdb",
        "internalConsoleOptions": "openOnSessionStart",

        "osx": {
            "MIMode": "lldb",
            "miDebuggerPath": "/usr/bin/lldb"
        },
        "windows": {
            "type": "cppvsdbg",
            "program": "${workspaceFolder}/build/.gen/test_run.exe"
        }
    }
  ]
}
