{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-kairo-current",
      "type": "shell",
      "command": "kairo",
      "args": [
        "${file}",
        "--debug",
        "-o",
        "${workspaceFolder}/build/.gens/dbg_run"
      ],
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
