{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-kairo-current",
      "type": "shell",
      "command": "kbld",
      "args": [
        "test",
        "${file}",
        "--compile-only",
      ],
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
            "PWD": "${workspaceFolder}",
            "CWD": "${workspaceFolder}",
        }
      },
      "promptOnClose": true,
    }
  ]
}