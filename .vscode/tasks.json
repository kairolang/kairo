{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-helix-current",
      "type": "shell",
      "command": "helix",
      "args": [
        "${file}",
        "--debug",
        "-o",
        "${workspaceFolder}/build/.gens/dbg_run"
      ],
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
